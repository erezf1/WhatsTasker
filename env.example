#secret keys
OPENAI_API_KEY=PUT KEY HERE 
GOOGLE_CLIENT_SECRET=PUT KEY HERE 
GOOGLE_CLIENT_ID=PUT KEY HERE 
TWILIO_AUTH_TOKEN=put the token here
#google integration params
GOOGLE_REDIRECT_URI=http://localhost:8000/oauth2callback  # FOR LOCAL
ENCRYPTION_KEY=OXUe-kwg5SVPms311IaXju7TkWukeQN4CtUzpLK4H3g=
BRIDGE_TYPE=cli

# Define the suffix for data files (registry, tokens, db)
# Use "_cli" for CLI mode, "" (empty) or "_wa" for WhatsApp mode
DATA_SUFFIX="_cli" # Example for local CLI .env
# DATA_SUFFIX="" # Example for VPS WhatsApp .env

# Define the port for the backend server
# Use different ports for different environments
PORT="8001" # Example for local CLI .env (different from default 8000)
# PORT="8000" # Example for VPS WhatsApp .env (keep default)

#set params for twilo integration
TWILIO_ACCOUNT_SID= put account number
TWILIO_WHATSAPP_NUMBER= put whatsapp number

# Set to "true" on the server where you want alerts active.
ALERT_EMAIL_ENABLED="true"
# The email address to send alerts TO.
ALERT_EMAIL_TO="erez@mvp-house.com"
# Google Workspace / Gmail SMTP server.
SMTP_HOST="smtp.gmail.com"
# Recommended port for TLS connection with Gmail.
SMTP_PORT="587"
# The Google Workspace email address that will SEND the alerts.
SMTP_USER="erez@mvp-house.com"
# !!! IMPORTANT: Generate a 16-digit Google App Password for the SMTP_USER account !!!
# !!! DO NOT use the regular account password. 2FA must be enabled for App Passwords. !!!
SMTP_PASS="bbyu tuqz qehy omfe"
# The "From" name displayed in the alert email (optional).
ALERT_FROM_NAME="WhatsTasker Alert"

# -- WhatsApp Bridge Error Handling --
CONSECUTIVE_ERROR_THRESHOLD="5"
# for mock_chat
VIEWER_PORT="5001"
VIEWER_ENABLED="true"

